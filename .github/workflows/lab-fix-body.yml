name: Lab fix script body

on:
  workflow_dispatch:
  push:
    paths:
      - scripts/lab_fix_body.py
      - .github/workflows/lab-fix-body.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/lab_fix_body.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dantix-vision-lab/index.html dantix-vision-lab/sw.js
          git diff --cached --stat
          git commit -m "Lab v224: quitar JS suelto al pie de la pagina" || echo sin-cambios
          git push
