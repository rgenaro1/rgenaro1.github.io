name: Prod reset puntos movil
on:
  workflow_dispatch:
  push:
    paths:
      - scripts/prod_reset_puntos.py
      - .github/workflows/prod-reset-puntos.yml
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/prod_reset_puntos.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dantix-vision/index.html
          git diff --cached --quiet && echo sin-cambios && exit 0
          git commit -m "Produccion: Reiniciar puntos y Quitar ultimo en VisionMeasure"
          git push
