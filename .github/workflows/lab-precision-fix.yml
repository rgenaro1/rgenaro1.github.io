name: Lab fix jsPDF leak

on:
  workflow_dispatch:
  push:
    paths:
      - scripts/lab_precision_fix.py
      - .github/workflows/lab-precision-fix.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 scripts/lab_precision_fix.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add dantix-vision-lab/index.html dantix-vision-lab/sw.js
          git diff --cached --stat
          git commit -m "Lab v224: quitar codigo jsPDF visible al final del home" || echo sin-cambios
          git push
